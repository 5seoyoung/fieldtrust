{
  "name": "fieldtrust-dev",
  "private": true,
  "description": "Dev harness for FieldTrust (the app itself is a dependency-free single-file index.html assembled from src/)",
  "scripts": {
    "build": "node scripts/build.js",
    "build:check": "node scripts/build.js --check",
    "test": "node scripts/build.js && node --test tests/test_core.js"
  },
  "devDependencies": {
    "fake-indexeddb": "^6.2.5",
    "jsdom": "^24.1.0"
  }
}
